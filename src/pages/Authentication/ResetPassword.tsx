import AuthenticationContainer from "containers/Authentication";
import React from "react";

const ResetPassword: React.FC = () => <AuthenticationContainer.ResetPassword />;

export default ResetPassword;
